import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    zIndex: 1000,
  },
  dropdown: {
    borderWidth: 1,
    paddingHorizontal: 16,
  },
  text: {
    fontSize: 16,
  },
});
