import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: 140,
    marginRight: 16,
    alignItems: 'center',
  },
  poster: {
    width: '100%',
    height: 200,
    borderRadius: 8,
  },
  title: {
    color: 'white',
    fontSize: 12,
    textAlign: 'center',
    marginTop: 6,
  },
});